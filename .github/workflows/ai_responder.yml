name: AI Profile Responder

on:
  issues:
    types: [opened]

permissions:
  issues: write

jobs:
  answer_question:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install Dependencies
        run: npm install openai @actions/core @actions/github
        
      - name: Generate Answer and Comment
        uses: actions/github-script@v6
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        with:
          script: |
            const { Configuration, OpenAIApi } = require("openai");

            // Initialize OpenAI
            // Note: Adjust depending on the openai version installed. 
            // For v4+:
            const OpenAI = require("openai");
            const openai = new OpenAI({
              apiKey: process.env.OPENAI_API_KEY,
            });

            const issue = context.payload.issue;
            const question = issue.title + "\n\n" + issue.body;

            console.log("Analyzing question: " + question.substring(0, 50) + "...");

            if (!process.env.OPENAI_API_KEY) {
              console.log("No OPENAI_API_KEY found.");
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issue.number,
                body: "⚠️ **Bot Error**: `OPENAI_API_KEY` is missing in repository secrets. Please configure it to enable AI responses."
              });
              return;
            }

            try {
              const completion = await openai.chat.completions.create({
                model: "gpt-3.5-turbo", // Or gpt-4 if the key supports it
                messages: [
                  { 
                    role: "system", 
                    content: `You are Ratheesh Kumar, a passionate Golang backend developer. 
                    - You Answer questions about Golang, PostgreSQL, MySQL, MongoDB, DevOps (Docker/K8s), and System Design.
                    - Be helpful, professional, but slightly casual and friendly.
                    - If the question is unrelated to tech or your profile, politely redirect them.
                    - Your email is ratheesh.k@hybridsolutions.com.` 
                  },
                  { role: "user", content: question }
                ],
                max_tokens: 300,
              });

              const answer = completion.choices[0].message.content;

              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issue.number,
                body: `## 🤖 AI Response\n\n${answer}\n\n---\n*This is an automated response based on my tech stack. For complex inquiries, email me directly!*`
              });

            } catch (error) {
              console.error(error);
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issue.number,
                body: "❌ **Error**: I couldn't process your request at the moment. Please try again later."
              });
            }
